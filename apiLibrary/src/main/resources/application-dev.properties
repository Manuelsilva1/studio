# Development Specific Configurations
# These properties will override application.properties when the 'dev' profile is active

# Example: Use an H2 in-memory database for dev for easier testing
# spring.datasource.url=jdbc:h2:mem:devdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.username=sa
# spring.datasource.password=
# spring.datasource.driver-class-name=org.h2.Driver
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=create-drop

server.port=8081 # Different port for dev if needed

logging.level.com.example.apiLibrary=DEBUG

# Dev mail server (e.g., MailHog or a test account)
# spring.mail.host=localhost
# spring.mail.port=1025
# spring.mail.username=
# spring.mail.password=
